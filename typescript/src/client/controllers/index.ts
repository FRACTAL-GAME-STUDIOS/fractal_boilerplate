import * as Utils from './../helpers/utils';
import * as Framework from './../../core/cl_bridge';

export const Init = async (): Promise<void> => {
	let playerData: any = Framework.GetPlayerData();
};